@model IEnumerable<PhoneBookApp.Models.Country>
@{
}

<div class="container">
    <a href="/Country/Add" class="btn btn-outline-warning">Add new country</a>
</div>

<div class="container pt-10">
    <div>
        <h2>Countries</h2>
        <table class="table table-stripped table-bordered">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.CountryName</td>
                        <td>
                            <a class="btn btn-outline-primary" href="/Country/Update?id=@item.Id">Edit</a>
                            <a class="btn btn-outline-danger" onclick="return window.confirm('Are you sure')" href="/Country/Delete?id=@item.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
